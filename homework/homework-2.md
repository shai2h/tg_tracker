Домашка #2 — Async, слои, схемы, логирование

Цель: переписать то что есть правильно. Новых фич минимум — фокус на архитектуре и async.

---

Пререквизиты

Смержить текущую ветку (чтобы сохранить домашку) и начать новую от ветки main:

Что реализовать

1. Async SQLAlchemy

Сейчас база синхронная, бот async — это блокировка event loop. Переведи всё на async:

- database.py — AsyncEngine, AsyncSession, async_sessionmaker
- crud.py — все функции async def, запросы через await
- main.py — все эндпоинты async def
- bot.py — убрать костыли, всё нативно async

DATABASE_URL теперь с +asyncpg:

    postgresql+asyncpg://user:password@localhost/tracker

---

2. Деньги в копейках

Пользователь присылает рубли (/add кофе 300), в БД хранится в копейках (30000).
(кстати, нам категории было бы здорово внедрить в будущем, обсудим как лучше)
Сделай маппинг в обе стороны:

- При создании: amount_kopecks = amount_rubles * 100
- При отдаче: amount_rubles = amount_kopecks / 100

Поле в модели переименовать в amount_kopecks чтобы было явно.

---

3. Недостающий эндпоинт — UPDATE

Был нужен ещё в домашке #1. Добавь PUT /expenses/{expense_id} и crud.update_expense().
круд - переименовать в репозиторий.

---

4. Роутеры вынести из main.py

Создай app/routers/expenses.py с APIRouter, подключи в main.py через app.include_router(...).
main.py должен остаться тонким — только инициализация приложения.

---

5. Все роуты — правильные схемы и коды

Пройдись по всем эндпоинтам:

- Каждый эндпоинт имеет response_model через Pydantic
- Правильные HTTP коды: 201 на создание, 204 на удаление, 404 если не найдено
- Валидация входных данных: amount не может быть отрицательным или нулём

---

6. Бот-сервис — вынести запуск в lifespan, переименовать файл

Сейчас bot.py содержит и хендлеры и запуск (main() + asyncio.run()).
Это два разных слоя — раздели их.

Задача:
- Запуск polling вынести в lifespan FastAPI (один процесс, один .env, одно подключение к БД)
- bot.py переименовать в app/bot-service.py (или app/bot/handlers.py — реши сам)
- Хендлеры остаются хендлерами: они валидируют ввод, ловят ошибки, ходят в репозиторий. Это и есть бот-сервис. 

Но: polling нужен только локально — в продакшне будут вебхуки.
Управляй этим через переменную окружения:

7. Логирование

Добавь логирование в проект. Что логировать — реши сам.
Подумай: что было бы полезно видеть когда что-то пошло не так?

---

Опционально (плюс в карму)

Перепиши .gitignore руками — только то что реально нужно проекту.
Без автогенерированного шума на 200 строк.

---

За пределами этой домашки — на следующий раз

- Миграции — create_all() в продакшне не используют, будем делать Alembic
- Деплой на VPS + вебхуки — E2E тестирование с реальным Telegram

---

Сдача

Ветка hw2, ссылка на PR.
